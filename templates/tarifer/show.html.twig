{% extends 'base.html.twig' %}

{% block title %}Tarifer{% endblock %}

{% block body %}
    <h1>Tarifer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tarifer.id }}</td>
            </tr>
            <tr>
                <th>Tarif</th>
                <td>{{ tarifer.tarif }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tarifer_index') }}">back to list</a>

    <a href="{{ path('app_tarifer_edit', {'id': tarifer.id}) }}">edit</a>

    {{ include('tarifer/_delete_form.html.twig') }}
{% endblock %}
